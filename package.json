{
  "name": "sidedash",
  "version": "1.0.0",
  "description": "Personal macOS menubar dashboard for running side-project CLI tools",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "start": "electron .",
    "test": "vitest run",
    "package": "electron-packager . sidedash --platform=darwin --out=dist --overwrite --prune --icon=build/icon.icns"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron/packager": "^20.0.4",
    "electron": "^34.5.8",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "menubar": "^9.5.2"
  }
}
